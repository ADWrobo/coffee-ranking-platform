export interface IRank {
    drink: string;
    cafe: string;
    location: string;
    rating: string;
    ratingRemainder: string; //This is the difference between the rating and 5 stars
    comment: string;
}